# دليل النشر السريع 🔧

دليل خطوة بخطوة لنشر مشروع تحليل رسائل التصيد على خوادم الاستضافة.

## 📋 خيارات النشر المتاحة

### 1️⃣ النشر على Render.com (مجاني - موصى به)

#### الخطوات:

1. **إنشاء حساب**:
   - اذهب إلى [render.com](https://render.com)
   - سجل حساب مجاني

2. **ربط المستودع**:
   - اضغط على "New" → "Web Service"
   - اختر "Connect GitHub" (أو GitLab/Bitbucket)
   - اختر المستودع الخاص بك

3. **إعدادات النشر**:
   - **Name**: `email-phishing-detector` (أو أي اسم تريده)
   - **Region**: اختر الأقرب لك (مثلاً Frankfurt)
   - **Branch**: `main` (أو الفرع الذي تريد نشره)
   - **Root Directory**: اتركه فارغاً
   - **Runtime**: Python 3
   - **Build Command**: 
     ```
     pip install --upgrade pip && pip install -r backend/requirements.txt
     ```
   - **Start Command**:
     ```
     cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
     ```

4. **النشر**:
   - اضغط "Create Web Service"
   - انتظر حتى ينتهي البناء (5-10 دقائق)
   - سيتم توفير رابط مثل: `https://email-phishing-detector.onrender.com`

#### ملاحظات:
- الخدمة المجانية تدخل في "نوم" بعد 15 دقيقة من عدم الاستخدام
- أول طلب قد يستغرق 30-60 ثانية لإيقاظ الخدمة

---

### 2️⃣ النشر على Railway (مجاني - سريع)

#### الخطوات:

1. **إنشاء حساب**:
   - اذهب إلى [railway.app](https://railway.app)
   - سجل بحساب GitHub

2. **النشر**:
   - اضغط "New Project"
   - اختر "Deploy from GitHub repo"
   - اختر المستودع الخاص بك
   - سيتم النشر تلقائياً! 🎉

3. **الحصول على الرابط**:
   - اضغط على الخدمة
   - انتقل إلى "Settings" → "Networking"
   - اضغط "Generate Domain" للحصول على رابط

#### ملاحظات:
- Railway يقرأ `railway.json` تلقائياً
- الخدمة المجانية تمنح 500 ساعة/شهر
- لا يوجد "نوم" للخدمات

---

### 3️⃣ النشر على Heroku

#### الخطوات:

1. **تثبيت Heroku CLI**:
   - من [heroku.com](https://devcenter.heroku.com/articles/heroku-cli)

2. **إنشاء التطبيق**:
   ```bash
   heroku login
   heroku create email-phishing-detector
   ```

3. **النشر**:
   ```bash
   git push heroku main
   ```

4. **فتح التطبيق**:
   ```bash
   heroku open
   ```

---

### 4️⃣ النشر باستخدام Docker

#### بناء الصورة:
```bash
docker build -t email-phishing-detector .
```

#### تشغيل الحاوية:
```bash
docker run -p 8000:8000 email-phishing-detector
```

#### النشر على Docker Hub:
```bash
docker tag email-phishing-detector username/email-phishing-detector
docker push username/email-phishing-detector
```

---

## 🔧 إعدادات مهمة بعد النشر

### 1. تحديث CORS

بعد النشر، احرص على تحديث `ALLOWED_ORIGINS` في إعدادات البيئة:

```bash
# على Render
ALLOWED_ORIGINS=https://your-app.onrender.com

# على Railway
ALLOWED_ORIGINS=https://your-app.railway.app
```

### 2. إعداد متغيرات البيئة (اختياري)

يمكنك إضافة متغيرات البيئة في لوحة التحكم:
- `PYTHONPATH`: المسار الصحيح للكود
- `ALLOWED_ORIGINS`: المجالات المسموحة

---

## ✅ التحقق من النشر

بعد النشر، تحقق من:

1. **فحص الصحة**:
   ```
   https://your-app-url.com/health
   ```
   يجب أن ترى: `{"status": "ok"}`

2. **فحص الواجهة**:
   ```
   https://your-app-url.com
   ```
   يجب أن ترى صفحة الويب الرئيسية

3. **فحص API**:
   ```
   https://your-app-url.com/api/analyze/text
   ```
   يجب أن يعمل التحليل

---

## 🐛 حل المشاكل الشائعة

### المشكلة: الخدمة لا تعمل
- **الحل**: تحقق من Logs في لوحة التحكم
- تأكد من أن `requirements.txt` موجود في `backend/`

### المشكلة: Frontend لا يظهر
- **الحل**: تأكد من وجود مجلد `frontend/` في المستودع
- تحقق من مسارات الملفات في `main.py`

### المشكلة: أخطاء Python
- **الحل**: تأكد من استخدام Python 3.12
- تأكد من تثبيت جميع المتطلبات

---

## 📞 المساعدة

إذا واجهت مشاكل:
1. تحقق من Logs في منصة الاستضافة
2. راجع ملف `README.md` للإرشادات التفصيلية
3. افتح Issue في المستودع

---

## 🎉 مبروك!

بعد النشر الناجح، سيكون لديك موقع حي لتحليل رسائل التصيد!

**رابط الموقع**: `https://your-app-url.com`

