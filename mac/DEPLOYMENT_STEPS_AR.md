# دليل تفصيلي لرفع ونشر المشروع 🚀

دليل شامل خطوة بخطوة لرفع المشروع إلى GitHub ونشره على منصة مجانية.

---

## الجزء الأول: رفع المشروع إلى GitHub

### الخطوة 1: إنشاء حساب على GitHub

1. اذهب إلى [github.com](https://github.com)
2. اضغط "Sign up"
3. املأ البيانات:
   - اسم المستخدم (Username)
   - البريد الإلكتروني
   - كلمة المرور
4. تحقق من البريد الإلكتروني

---

### الخطوة 2: تثبيت Git على جهازك

#### على Windows:
1. اذهب إلى [git-scm.com/download/win](https://git-scm.com/download/win)
2. حمّل Git for Windows
3. شغّل المثبت واتبع التعليمات
4. اضغط "Next" حتى النهاية

#### التحقق من التثبيت:
افتح PowerShell أو CMD واكتب:
```bash
git --version
```
إذا ظهر إصدار Git، تم التثبيت بنجاح ✅

---

### الخطوة 3: إعداد Git (أول مرة فقط)

افتح PowerShell في مجلد المشروع (`C:\Users\W K\Downloads\mac`):

```bash
# إعداد اسم المستخدم
git config --global user.name "Your Name"

# إعداد البريد الإلكتروني
git config --global user.email "your.email@example.com"
```

استبدل:
- `"Your Name"` باسمك الحقيقي
- `"your.email@example.com"` ببريدك على GitHub

---

### الخطوة 4: إنشاء مستودع جديد على GitHub

1. سجل دخول إلى [github.com](https://github.com)
2. اضغط على أيقونة "+" في الزاوية اليمنى العليا
3. اختر "New repository"
4. املأ المعلومات:
   - **Repository name**: `email-phishing-detector` (أو أي اسم تريده)
   - **Description**: `نظام تحليل رسائل التصيد باستخدام تعلم الآلة`
   - **Visibility**: اختر **Public** (للاستخدام المجاني)
   - **لا** تضع علامة على "Initialize this repository with a README"
5. اضغط "Create repository"

---

### الخطوة 5: رفع الملفات إلى GitHub

افتح PowerShell في مجلد المشروع:

```bash
# الانتقال إلى مجلد المشروع
cd "C:\Users\W K\Downloads\mac"

# تهيئة Git في المشروع
git init

# إضافة جميع الملفات
git add .

# إنشاء commit (نسخة أولية)
git commit -m "Initial commit: Email Phishing Detector"

# إضافة المستودع البعيد (استبدل YOUR_USERNAME باسمك على GitHub)
git remote add origin https://github.com/YOUR_USERNAME/email-phishing-detector.git

# رفع الملفات إلى GitHub
git branch -M main
git push -u origin main
```

**ملاحظة مهمة**: عند أول push، قد يُطلب منك:
1. اسم المستخدم على GitHub
2. كلمة المرور (أو Personal Access Token)

#### إنشاء Personal Access Token (إذا طُلب):
1. اذهب إلى GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. اضغط "Generate new token (classic)"
3. أعطِه اسماً مثل "Project Upload"
4. حدد صلاحيات: **repo** (كامل)
5. اضغط "Generate token"
6. انسخ الرمز واحفظه (لن يظهر مرة أخرى)
7. استخدم هذا الرمز كـ "كلمة المرور" عند push

---

### الخطوة 6: التحقق من الرفع

1. اذهب إلى المستودع على GitHub:
   `https://github.com/YOUR_USERNAME/email-phishing-detector`
2. يجب أن ترى جميع الملفات:
   - ✅ `backend/`
   - ✅ `frontend/`
   - ✅ `Dockerfile`
   - ✅ `README.md`
   - ✅ `render.yaml`
   - إلخ...

---

## الجزء الثاني: النشر على Render.com (مجاني)

### الخطوة 1: إنشاء حساب على Render

1. اذهب إلى [render.com](https://render.com)
2. اضغط "Get Started for Free"
3. اختر "Sign up with GitHub"
4. سُمح لـ Render بالوصول إلى مستودعاتك

---

### الخطوة 2: إنشاء Web Service

1. في لوحة التحكم، اضغط "New +" → "Web Service"
2. اختر "Connect GitHub" إذا لم يكن متصل
3. اختر المستودع `email-phishing-detector`
4. اضغط "Connect"

---

### الخطوة 3: إعدادات النشر

املأ النموذج بالإعدادات التالية:

#### الإعدادات الأساسية:
- **Name**: `email-phishing-detector` (أو أي اسم تريده)
- **Region**: اختر الأقرب لك (مثلاً `Frankfurt` أو `Oregon`)
- **Branch**: `main`
- **Root Directory**: اتركه فارغاً

#### إعدادات البيئة:
- **Environment**: `Python 3`
- **Build Command**:
  ```
  pip install --upgrade pip && pip install -r backend/requirements.txt
  ```
- **Start Command**:
  ```
  cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
  ```

#### متغيرات البيئة (Environment Variables):
اضغط "Advanced" ثم "Add Environment Variable":
- **Key**: `PYTHONPATH`
- **Value**: `/opt/render/project/src`

---

### الخطوة 4: النشر

1. اضغط "Create Web Service"
2. انتظر 5-10 دقائق حتى ينتهي البناء
3. راقب Logs للتأكد من نجاح البناء

---

### الخطوة 5: الحصول على الرابط

بعد نجاح النشر:
1. ستحصل على رابط مثل: `https://email-phishing-detector.onrender.com`
2. اضغط على الرابط لفتح الموقع
3. يجب أن ترى الواجهة الرئيسية ✅

---

## الجزء الثالث: النشر على Railway (بديل)

### الخطوة 1: إنشاء حساب على Railway

1. اذهب إلى [railway.app](https://railway.app)
2. اضغط "Start a New Project"
3. اختر "Deploy from GitHub repo"
4. سُمح لـ Railway بالوصول إلى مستودعاتك

### الخطوة 2: اختيار المستودع

1. اختر مستودع `email-phishing-detector`
2. اضغط "Deploy Now"
3. سيقوم Railway بالنشر تلقائياً!

### الخطوة 3: الحصول على الرابط

1. اضغط على المشروع
2. انتقل إلى "Settings" → "Networking"
3. اضغط "Generate Domain"
4. ستحصل على رابط مثل: `https://email-phishing-detector-production.up.railway.app`

---

## اختبار النشر ✅

بعد النشر، اختبر:

### 1. فحص الصحة:
```
https://your-url.com/health
```
**النتيجة المتوقعة**: `{"status": "ok"}`

### 2. فحص الواجهة:
```
https://your-url.com
```
**النتيجة المتوقعة**: صفحة الويب الرئيسية

### 3. اختبار التحليل:
- افتح الموقع
- الصق نص رسالة بريدية
- اضغط "Analyze Text"
- يجب أن تحصل على نتيجة ✅

---

## تحديث المشروع لاحقاً

عند تعديل الملفات محلياً:

```bash
# الانتقال إلى مجلد المشروع
cd "C:\Users\W K\Downloads\mac"

# إضافة التغييرات
git add .

# إنشاء commit
git commit -m "وصف التغييرات"

# رفع التغييرات
git push
```

**ملاحظة**: Render و Railway سيحدثان الموقع تلقائياً عند كل push! 🎉

---

## حل المشاكل الشائعة

### المشكلة: Git لا يعمل
**الحل**: تأكد من تثبيت Git وقم بإعادة فتح PowerShell

### المشكلة: لا يمكن رفع الملفات
**الحل**: 
- تأكد من اسم المستخدم والبريد في `git config`
- استخدم Personal Access Token بدلاً من كلمة المرور

### المشكلة: البناء فشل على Render
**الحل**:
- تحقق من Logs في Render
- تأكد من وجود `backend/requirements.txt`
- تأكد من أن `Start Command` صحيح

### المشكلة: الموقع لا يعمل
**الحل**:
- تحقق من Logs
- تأكد من أن البناء نجح
- تأكد من أن جميع الملفات موجودة

---

## روابط مفيدة 🔗

- **GitHub**: [github.com](https://github.com)
- **Render**: [render.com](https://render.com)
- **Railway**: [railway.app](https://railway.app)
- **Git Documentation**: [git-scm.com/doc](https://git-scm.com/doc)

---

## ملخص سريع 📋

1. ✅ تثبيت Git
2. ✅ إعداد Git (اسم وبريد)
3. ✅ إنشاء مستودع على GitHub
4. ✅ رفع الملفات (`git add .`, `git commit`, `git push`)
5. ✅ النشر على Render أو Railway
6. ✅ اختبار الموقع

---

**مبروك! 🎉** بعد اتباع هذه الخطوات، سيكون لديك موقع حي على الإنترنت!

